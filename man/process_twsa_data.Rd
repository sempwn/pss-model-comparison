% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twsa_plots.r
\name{process_twsa_data}
\alias{process_twsa_data}
\title{Process data for sensitivity analysis plotting}
\usage{
process_twsa_data(data_obj)
}
\arguments{
\item{data_obj}{A data frame or matrix containing TWSA result values,
with columns \code{n_inc_odf_scaled} and \code{n_inc_odn_scaled}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{x_scale} — numeric x-coordinate for the grid
\item \code{y_scale} — numeric y-coordinate for the grid
\item \code{n_inc_odf_scaled} — scaled incremental overdoses (ODF)
\item \code{n_inc_odn_scaled} — scaled incremental overdoses (ODN)
\item \code{incremental_overdoses_scaled} — total scaled incremental overdoses
}
}
\description{
Takes a TWSA result object and formats it into a tibble with \code{x_scale} and
\code{y_scale} coordinates covering a full square grid from -1 to 1, suitable
for plotting with \code{twsa_heatmap_plot()}.
}
\details{
The function generates a 17x17 grid (289 points) for plotting, corresponding
to combinations of x and y scaling factors from -1 to 1. The column
\code{incremental_overdoses_scaled} is computed as the sum of \code{n_inc_odf_scaled}
and \code{n_inc_odn_scaled}.
}
\examples{
\dontrun{
processed <- process_twsa_data(my_results)
head(processed)
}

}
