% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_outcome_by_quarter}
\alias{plot_outcome_by_quarter}
\title{Plot Outcome by Year-Quarter and Model}
\usage{
plot_outcome_by_quarter(plot_data, outcome = "PSS drug_deaths")
}
\arguments{
\item{plot_data}{A data frame containing at least the columns \code{year_quarter},
the specified \code{outcome}, and \code{model}. The \code{year_quarter} column should be
a factor or character indicating time periods, and \code{model} should specify
the model group.}

\item{outcome}{A character string specifying the name of the outcome column
in \code{plot_data} to be plotted. Defaults to \code{"PSS drug_deaths"}.}
}
\value{
A \code{ggplot2} object representing the boxplot of the specified outcome
by \code{year_quarter} and \code{model}.
}
\description{
Creates a boxplot of a specified outcome variable by \code{year_quarter},
grouped and colored by \code{model}. This is useful for comparing outcomes
(e.g., overdose deaths) across different models over time.
}
\examples{
\dontrun{
plot_data <- data.frame(
  year_quarter = rep(c("2020 Q1", "2020 Q2"), each = 10),
  model = rep(c("Model A", "Model B"), times = 10),
  `PSS drug_deaths` = rpois(20, 10)
)
plot_outcome_by_quarter(plot_data)
}

}
