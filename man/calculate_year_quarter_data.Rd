% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_data.R
\name{calculate_year_quarter_data}
\alias{calculate_year_quarter_data}
\title{Summarize Model Data by Year-Quarter}
\usage{
calculate_year_quarter_data(model_data)
}
\arguments{
\item{model_data}{A data frame containing model output. Must include columns:
\itemize{
\item \code{date}: a Date object,
\item \code{year_quarter}: a grouping variable (e.g., "2021 Q2"),
\item \code{model}: model identifier,
\item \code{run}: simulation run identifier,
along with numeric variables to be summarized.
}}
}
\value{
A grouped and summarized tibble with one row per combination of \code{year_quarter}, \code{model}, and \code{run}.
All non-Date columns are summed. \code{year_quarter} is returned as a factor.
}
\description{
Filters, groups, and summarizes model output data by year-quarter, model, and run.
}
\details{
This function takes a data frame of model output, filters out dates before March 1, 2020,
groups the data by \code{year_quarter}, \code{model}, and \code{run}, then sums all numeric (non-Date) columns
within those groups. The \code{year_quarter} variable is converted to a factor.
}
\examples{
\dontrun{
calculate_year_quarter_data(model_data)
}
}
